{
  "// note": "EXAMPLE — merge the dedicated `gitlab-review` server block into your own VS Code .vscode/mcp.json or user settings. Do not overwrite an existing file. The package is pinned for reproducibility; upgrade it deliberately after compatibility testing. The server exposes read-only MR, repository, pipeline, issue, and work-item evidence plus exactly two confirmed writes: a new review thread and a new MR summary note. Use a short-lived token with the minimum project role required to comment.",
  "// hardening": "Optionally add GITLAB_ALLOWED_PROJECT_IDS (comma-separated numeric project IDs) to the env block to hard-limit which projects the server can reach, and export the same token as GITLAB_TOKEN in your shell so the collect-mr-evidence.py script can run its single read-only evidence pass.",
  "servers": {
    "gitlab-review": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@zereight/mcp-gitlab@2.1.28"],
      "env": {
        "GITLAB_PERSONAL_ACCESS_TOKEN": "${input:gitlabPat}",
        "GITLAB_API_URL": "https://gitlab.yourco.com/api/v4",
        "GITLAB_READ_ONLY_MODE": "false",
        "GITLAB_TOOLSETS": "merge_requests,repositories,pipelines",
        "GITLAB_TOOLS": "get_issue,get_work_item",
        "GITLAB_DENIED_TOOLS_REGEX": "^(?!get_|list_|search_|validate_|mr_discussions$|create_merge_request_thread$|create_merge_request_note$).*",
        "GITLAB_TOOL_POLICY_APPROVE": "create_merge_request_thread,create_merge_request_note",
        "USE_GITLAB_WIKI": "false",
        "USE_MILESTONE": "false",
        "USE_PIPELINE": "true"
      }
    }
  },
  "inputs": [
    {
      "id": "gitlabPat",
      "type": "promptString",
      "description": "Short-lived GitLab token for MR review (api scope)",
      "password": true
    }
  ]
}
